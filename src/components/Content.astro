---
interface UserData {
  id: string
  name: string
  apellido: string
  correo: string
  rol: string
}
/*
const data: UserData[] = await fetch("http://localhost:3000/users")
  .then(res => res.json())
  */
---

<section>
  <article>
    {/* 
      data.map((user) => (
        <div class="info-container">
          <div>
            <span>Nombre:</span>
            <p>{user.name}</p>
          </div>
          <div>
            <span>Apellido:</span>
            <p>{user.apellido}</p>
          </div>
          <div>
            <span>Correo:</span>
            <p>{user.correo}</p>
          </div>
          <div>
            <span>Rol:</span>
            <p>{user.rol}</p>
          </div>
        </div>
      ))
    }
      */}
    <div class="info-container">
      <div>
        <span>Nombre</span>
        <p>Pepe</p>
      </div>
      <div>
        <span>Apellido</span>
        <p>Ruiz</p>
      </div>
      <div>
        <span>Rol</span>
        <p class="info-rol">Trabajador</p>
      </div>
    </div>

    <div class="info-container">
      <div>
        <span>Nombre</span>
        <p>Pepe</p>
      </div>
      <div>
        <span>Apellido</span>
        <p>Ruiz</p>
      </div>
      <div>
        <span>Rol</span>
        <p class="info-rol">Trabajador</p>
      </div>
    </div>

    <div class="info-container">
      <div>
        <span>Nombre</span>
        <p>Pepe</p>
      </div>
      <div>
        <span>Apellido</span>
        <p>Ruiz</p>
      </div>
      <div>
        <span>Rol</span>
        <p class="info-rol">Trabajador</p>
      </div>
    </div>
  </article>
</section>

<style>
  section {
    margin-top: 30px;
  }

  article {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(47%, 1fr));
    align-items: center;
  }
  
  .info-container {
    justify-items: center;
    margin: 25px;
    border-radius: 10px;
    padding: 5px 20px;
    background-color: #2c2b2bdd;
  }

  div {
    width: 80%;
    text-align: center;
    margin-block: 20px;

    & span {
      font-weight: 800;
      margin-right: 10px;
      width: 35%;
    }

    & p {
      width: 65%;
      margin: 0;
      display: inline-block;
      text-align: center;
    }

    & .info-rol {
      color: #646464fc;
    }
  }
</style>