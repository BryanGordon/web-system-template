---
import Layout from '../../layouts/Layout.astro'

export const prerender = false

const { id } = Astro.params
const response = await fetch("")
const product = await response.json()
---

<Layout>
  <h1>Hola buenas tardes { id }</h1>
</Layout>